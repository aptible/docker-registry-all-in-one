# NOTE: This configuration is incomplete and does not include a storage driver.
# One must be provided via the environment.
version: 0.1

log:
  fields:
    service: registry

http:
  addr: 127.0.0.1:5000
  headers:
    X-Content-Type-Options: [nosniff]
